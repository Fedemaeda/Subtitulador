[project]
name = "subtitulador"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "faster-whisper",
    "flask",
    "torch",
    "pywebview",
]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"

[tool.uv.sources]
torch = { index = "pytorch-cpu" }
